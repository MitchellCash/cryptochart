---
title: Top Open-Source Cryptocurrencies
---
<%= partial "includes/navbar", :locals => {:controls => true} %>
<div class="main landing">
  <ul class="projects">
    <% projects.each_with_index do |project, index| %>
      <li class="project lang-<%= project.language.split(/\s*,\s*/).map { |l| l.downcase.gsub(/\s/, '-') }.join(' lang-') %>"
          data-stars="<%= project.stars %>"
          data-forks="<%= project.forks %>"
          data-issues="<%= project.issues %>"
          data-title="<%= project.title %>"
          data-language="<%= project.language.downcase.gsub(/\s/, '-') %>">
        <div class="card">
          <h4 class="title"><a href="<%= project.url %>"><%= project.title %></a></h4>
          <h6 class="url"><a href="<%= project.homepage %>"><%= project.homepage.sub(/^https?:\/\//, '') %></a></small></h6>
          <ul class="stats">
            <li title="Stars">
              <i class="fa fa-star"></i>
              <span class="stat"><%= project.stars %></span>
              <span class="trend <%= project.stars_trend %>" title="Difference since last week">
                <%= project.stars_diff %>
                <%= caret_icon(project.stars_diff) %>
              </span>
            </li>
            <li title="Forks">
              <i class="fa fa-code-fork"></i>
              <span class="stat"><%= project.forks %></span>
              <span class="trend <%= project.forks_trend %>" title="Difference since last week">
                <%= project.forks_diff %>
                <%= caret_icon(project.forks_diff) %>
              </span>
            </li>
            <li title="Open Issues">
              <i class="fa fa-bug"></i>
              <span class="stat"><%= project.issues %></span>
              <span class="trend" title="Difference since last week">
                <%= project.issues_diff %>
                <%= caret_icon(project.issues_diff) %>
              </span>
            </li>
          </ul>
          <div class="description"><%= project.description %></div>
          <dl>
            <dt>Language:</dt>
            <dd><%= project.language %></dd>

            <dt>License:</dt>
            <dd><%= project.license %></dd>
          </dl>
        </div>
        <% if index == 2 %>
          <li id="deploy-to-netlify" class="project deploy-to-netlify">
            <h3>Missing a cryptocurrency?</h3>
            <p>If you would like to see your favourite cryptocurrency tracked by CryptoChart, lodge a request <a href="https://github.com/mitchellcash/cryptochart/issues/new">here</a>.<br><br>
            Want to see your favourite cryptocurrency featured in top position? Send an <a href="mailto:&#105;&#110;&#102;&#111;&#064;&#099;&#114;&#121;&#112;&#116;&#111;&#099;&#104;&#097;&#114;&#116;&#046;&#105;&#111;?subject=Enquiry about promoting a cryptocurrency">email</a> to enquire about paid positions.</p>
          </li>
        <% end %>
      </li>
    <% end %>
  </ul>
</div>
